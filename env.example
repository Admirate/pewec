# =============================================================================
# Supabase
# Run `supabase status` after `supabase start` to get local dev values.
# =============================================================================

# Public (exposed to the browser)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only — never expose to the browser
SUPABASE_SERVICE_ROLE_KEY=

# =============================================================================
# Email
# In development, emails are sent via Mailpit (bundled with supabase start).
#   Web UI: http://127.0.0.1:54324
#   SMTP:   127.0.0.1:54325 (no auth required — NODE_ENV=development auto-selects this)
#
# In production, emails are sent via Resend.
#   Sign up at https://resend.com and add your API key below.
# =============================================================================

# Production only — ignored when NODE_ENV=development
RESEND_API_KEY=re_...

# The "From" address used for all outgoing emails.
# Defaults to noreply@pewec.com if unset.
EMAIL_FROM=noreply@pewec.com
